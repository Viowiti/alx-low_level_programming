#include <stdio.h>

/**
 *main - prints the string using the puts function to the standard output
 *
 *Return: the integer 0 on successful compilation
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
