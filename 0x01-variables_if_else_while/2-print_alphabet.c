#include <stdio.h>

/**
 *main - prints the lower alphabet.
 *
 *Return: returns 0 uponsuccessful completion"
 */

int main(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
