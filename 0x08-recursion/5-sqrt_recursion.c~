#include <unistd.h>
#include "holberton.h"
/**
 * _sqrt_recursion - returns the natural square root of a number
 * @n: number lookin for root
 *
 * Return: On success 0
 * On error,1 is returned, and errno is set appropriately.
 */
int _sqrt_recursion(int n)
{
